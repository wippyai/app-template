PUBLIC_API_URL=http://localhost:8080

# Sentry error tracking (optional — set DSN to enable)
# PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0


USERSPACE_USER_DEFAULT_ADMIN_EMAIL=admin@wippy.local
# Required. The default admin password must be set explicitly for every deployment.
# Startup will fail if this value is unset or left as empty.
USERSPACE_USER_DEFAULT_ADMIN_PASSWORD=

# OpenAI-compatible provider (supports OpenRouter, Ollama, vLLM, etc.)
OPENAI_COMPAT_API_KEY=your-api-key-here
OPENAI_COMPAT_BASE_URL=https://openrouter.ai/api/v1